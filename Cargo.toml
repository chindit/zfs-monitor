[package]
name = "zfs-monitor" # the name of the package
version = "0.1.0"    # the current version, obeying semver
authors = ["David Lumaye <littletiger58@gmail.com>"]

[dependencies]
lettre = "0.10.0-rc.3"
dotenv = "0.15.0"